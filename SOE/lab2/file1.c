#include <stdio.h>
int main()
{
    int number;

    printf("Enter an ");
    scanf("%d", &number);
    if (number < 0)
    {
        printf("You entered %d.\n", number);
    }
    for(int i=0; i<n; i++){ 
	}
    while(n--){
	printf("d");
}
    return 0;
}
